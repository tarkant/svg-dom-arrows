export interface ArrowPath {
    offset: Offset;
    size:   Size;
    points: string;
}

export interface Offset {
    x: number;
    y: number;
}

export interface Size {
    width:  number;
    height: number;
}
